#include <iostream>

int main(int argc, char* argv[])
{
    // Initialize library.
    //openfpm_init(&argc, &argv);

    std::cout << "Hello World!" << std::endl;

    //openfpm_finalize();
    return 0;
}
